for SWI-Prolog, using Macports, and GCC instead of Clang

mkdir build
cd build
cmake -G Ninja -DCMAKE_C_COMPILER=gcc -DCMAKE_CXX_COMPILER=g++ -DCMAKE_INSTALL_PREFIX=/opt/local ..
ninja
sudo ninja install

